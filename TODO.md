# TODO

- [ ] Inspect current `lib/screens/counselors_screen.dart` and identify the “counselor selection part” that must remain.
- [ ] Restore the counselors screen UI to the original version by replacing the current allocation-request flow with only the counselor selection UI.
- [ ] Ensure the rest of the screen (navigation/header/bottom bar) stays compatible.
- [ ] Run `flutter analyze` and (if possible) `flutter test` to confirm no build errors.

