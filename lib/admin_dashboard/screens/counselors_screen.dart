import 'package:flutter/material.dart';

class CounselorsScreen extends StatelessWidget {
  const CounselorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Counselor Management"));
  }
}